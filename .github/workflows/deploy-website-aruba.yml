name: Deploy IMC public website to Aruba

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: imc-public-website-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Inject GW001 build ID
        shell: bash
        run: |
          set -euo pipefail
          sed -i "s/BUILD_ID/${GITHUB_SHA}/g" gameworld001/index.html
          grep -Fq "app.js?v=${GITHUB_SHA}" gameworld001/index.html
          grep -Fq "menu.js?v=${GITHUB_SHA}" gameworld001/index.html
          grep -Fq "menu.css?v=${GITHUB_SHA}" gameworld001/index.html
          grep -Fq "competition-table.js?v=${GITHUB_SHA}" gameworld001/index.html
          grep -Fq "competition-table.css?v=${GITHUB_SHA}" gameworld001/index.html
          grep -Fq "competition-groups.js?v=${GITHUB_SHA}" gameworld001/index.html
          grep -Fq "competition-groups.css?v=${GITHUB_SHA}" gameworld001/index.html

      - name: Validate package
        shell: bash
        run: |
          set -euo pipefail
          test -s index.html
          test -s site-assets/css/imc-site.css
          test -s site-assets/js/imc-site.js
          test -s site-assets/js/world-feed.js
          test -s site-assets/images/imc-logo.png
          test -s gameworld001/index.html
          test -s gameworld001/app.js
          test -s gameworld001/menu.js
          test -s gameworld001/menu.css
          test -s gameworld001/competition-detail.js
          test -s gameworld001/competition-detail.css
          test -s gameworld001/competition-table.js
          test -s gameworld001/competition-table.css
          test -s gameworld001/competition-groups.js
          test -s gameworld001/competition-groups.css
          test -s gameworld001/team-hub.js
          test -s gameworld001/style.css
          test -s gameworld001/.htaccess
          test -s api/imc-gateway/team-hub.php
          grep -Fq 'GW001 · Road To History' gameworld001/index.html
          grep -Fq './app.js?v=' gameworld001/index.html
          grep -Fq './menu.js?v=' gameworld001/index.html
          grep -Fq './menu.css?v=' gameworld001/index.html
          grep -Fq './competition-detail.js?v=' gameworld001/index.html
          grep -Fq './competition-detail.css?v=' gameworld001/index.html
          grep -Fq './competition-table.js?v=' gameworld001/index.html
          grep -Fq './competition-table.css?v=' gameworld001/index.html
          grep -Fq './competition-groups.js?v=' gameworld001/index.html
          grep -Fq './competition-groups.css?v=' gameworld001/index.html
          grep -Fq './team-hub.js?v=' gameworld001/index.html
          grep -Fq './style.css?v=' gameworld001/index.html

      - name: Validate Aruba secrets
        env:
          ARUBA_FTP_USERNAME: ${{ secrets.ARUBA_FTP_USERNAME }}
          ARUBA_FTP_PASSWORD: ${{ secrets.ARUBA_FTP_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          for name in ARUBA_FTP_USERNAME ARUBA_FTP_PASSWORD; do test -n "${!name:-}"; done

      - name: Upload website and GW001 structure
        env:
          ARUBA_FTP_USERNAME: ${{ secrets.ARUBA_FTP_USERNAME }}
          ARUBA_FTP_PASSWORD: ${{ secrets.ARUBA_FTP_PASSWORD }}
        shell: bash
        run: |
          python3 - <<'PY'
          import io, os, ssl, time
          from ftplib import FTP_TLS, error_perm
          from pathlib import Path
          host='ftp.italianmastersclub.it'; port=21
          username=os.environ['ARUBA_FTP_USERNAME']; password=os.environ['ARUBA_FTP_PASSWORD']
          remote_root='/www.italianmastersclub.it'
          ctx=ssl.create_default_context(); ctx.check_hostname=False
          ftp=None; last_error=None
          for _ in range(3):
              c=FTP_TLS(context=ctx)
              try:
                  c.connect(host,port,timeout=60); c.login(username,password); c.prot_p(); c.set_pasv(True); ftp=c; break
              except Exception as e:
                  last_error=e
                  try: c.close()
                  except Exception: pass
                  time.sleep(5)
          if ftp is None: raise SystemExit(f'FTPS Aruba non riuscito: {type(last_error).__name__}: {last_error}')
          def ensure_dir(remote_path):
              ftp.cwd('/')
              for part in [p for p in remote_path.split('/') if p]:
                  try: ftp.cwd(part)
                  except error_perm: ftp.mkd(part); ftp.cwd(part)
          excluded={'site-assets/css/game-world.css','site-assets/css/gameworld-engine.css','site-assets/js/gameworld-engine.js','site-assets/js/imc-data-service.js','site-assets/js/team-hub-detail.js'}
          files=[Path('index.html')]
          files += [p for p in sorted(Path('site-assets').rglob('*')) if p.is_file() and p.as_posix() not in excluded]
          files += [
              Path('gameworld001/index.html'),
              Path('gameworld001/app.js'),
              Path('gameworld001/menu.js'),
              Path('gameworld001/menu.css'),
              Path('gameworld001/competition-detail.js'),
              Path('gameworld001/competition-detail.css'),
              Path('gameworld001/competition-table.js'),
              Path('gameworld001/competition-table.css'),
              Path('gameworld001/competition-groups.js'),
              Path('gameworld001/competition-groups.css'),
              Path('gameworld001/team-hub.js'),
              Path('gameworld001/style.css'),
              Path('gameworld001/.htaccess'),
              Path('api/imc-gateway/team-hub.php')
          ]
          for local in files:
              rel=local.as_posix(); parent=local.parent.as_posix(); dest=remote_root if parent=='.' else f"{remote_root.rstrip('/')}/{parent}"
              ensure_dir(dest); data=local.read_bytes()
              with local.open('rb') as fh: ftp.storbinary(f'STOR {local.name}',fh)
              check=io.BytesIO(); ftp.retrbinary(f'RETR {local.name}',check.write)
              if check.getvalue()!=data: raise SystemExit(f'Verifica upload fallita: {rel}')
          ftp.quit(); print(f'Pubblicati e verificati {len(files)} file')
          PY

      - name: Verify GW001 online
        shell: bash
        run: |
          set -euo pipefail
          token="${GITHUB_SHA}-$(date +%s)"
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/?verify=$token" -o /tmp/gw001.html
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/app.js?verify=$token" -o /tmp/gw001-app.js
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/menu.js?verify=$token" -o /tmp/gw001-menu.js
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/menu.css?verify=$token" -o /tmp/gw001-menu.css
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/competition-detail.js?verify=$token" -o /tmp/gw001-competition-detail.js
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/competition-detail.css?verify=$token" -o /tmp/gw001-competition-detail.css
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/competition-table.js?verify=$token" -o /tmp/gw001-competition-table.js
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/competition-table.css?verify=$token" -o /tmp/gw001-competition-table.css
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/competition-groups.js?verify=$token" -o /tmp/gw001-competition-groups.js
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/competition-groups.css?verify=$token" -o /tmp/gw001-competition-groups.css
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/gameworld001/team-hub.js?verify=$token" -o /tmp/gw001-team-hub.js
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/api/imc-gateway/team-hub.php?game_world_id=GW001&dataset=clubs&verify=$token" -o /tmp/gw001-clubs.json
          curl -fsSL -H 'Cache-Control: no-cache, no-store' "https://www.italianmastersclub.it/api/imc-gateway/team-hub.php?game_world_id=GW001&dataset=nations&verify=$token" -o /tmp/gw001-nations.json
          grep -Fq "app.js?v=${GITHUB_SHA}" /tmp/gw001.html
          grep -Fq "menu.js?v=${GITHUB_SHA}" /tmp/gw001.html
          grep -Fq "menu.css?v=${GITHUB_SHA}" /tmp/gw001.html
          grep -Fq "competition-table.js?v=${GITHUB_SHA}" /tmp/gw001.html
          grep -Fq "competition-table.css?v=${GITHUB_SHA}" /tmp/gw001.html
          grep -Fq "competition-groups.js?v=${GITHUB_SHA}" /tmp/gw001.html
          grep -Fq "competition-groups.css?v=${GITHUB_SHA}" /tmp/gw001.html
          grep -Fq 'Tommaso Mello' /tmp/gw001-app.js
          grep -Fq 'manager IMC attivi' /tmp/gw001-app.js
          grep -Fq "const NAV" /tmp/gw001-menu.js
          grep -Fq '.global-nav{display:none!important}' /tmp/gw001-menu.css
          grep -Fq 'competition-detail-tabs' /tmp/gw001-competition-detail.js
          grep -Fq '.competition-detail-tabs' /tmp/gw001-competition-detail.css
          grep -Fq 'buildStandings' /tmp/gw001-competition-table.js
          grep -Fq '.cd-standings-table' /tmp/gw001-competition-table.css
          grep -Fq 'competition-group-hero-card' /tmp/gw001-competition-groups.js
          grep -Fq '.competition-group-hero-card' /tmp/gw001-competition-groups.css
          grep -Fq 'CORE Aruba' /tmp/gw001-team-hub.js
          python3 - <<'PY'
          import json
          clubs=json.load(open('/tmp/gw001-clubs.json')); nations=json.load(open('/tmp/gw001-nations.json'))
          assert clubs.get('ok') is True and clubs.get('count') == 40, clubs
          assert nations.get('ok') is True and nations.get('count') == 80, nations
          assert any(x.get('name') == 'FC Kaiserslautern' for x in clubs.get('data', [])), clubs
          print('GW001 Team Hub online: 40 clubs / 80 nations')
          PY
          echo "GW001 build ${GITHUB_SHA} online OK"
